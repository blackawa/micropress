(ns micropress.service.draft
  (:require [clj-time.core :as c]
            [clj-time.format :as f]
            [micropress.repository :as r]
            [micropress.util.encrypt :as ecp]))

(defn save-draft
  [title content thumbnail-url body-type tags user-id]
  )
